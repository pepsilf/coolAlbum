<pop-manager
  show="{{ show }}"
  type="{{ type }}"
  show-overlay="{{ overlay }}"
  bindclickmask="handleMaskClick">
  <view class='popper-dialog_body--{{type}}'>
    <slot></slot>
  </view>
</pop-manager>
